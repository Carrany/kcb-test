import { Button, Input } from "antd";
import styled from "styled-components";

export const AppModalCreateUserInput = styled(Input)`
  width: 100%;
`;

export const AppModalCreateUserButton = styled(Button)`
  width: 100%;
  margin-top: 20px;
`;

